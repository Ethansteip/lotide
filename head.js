/*
* head - returns the element at index zero in a given array.
*
* @param {array} array - the first array that we will search for element at index 0.
*/

const head = function(array) {

  return array[0];

};

module.exports = head;




